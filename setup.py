from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='DSR Batch 30 portfolio project: identify, analyze and visualize physical processes in videos.',
    author='Simon Markus',
    license='',
)
